1 -> Implement a file uploader input to upload user avatar (Done).

2 -> Implement a attachement input uploader to send attachements(skip for now).

3 -> Make the chat Dashboard responsive for different screen size(skip for now).

4 -> Display the last message between the users on their respective Friends card.

5 -> Add pagination to load the previous chats in the UI.

6 -> Add the single tick, double tick and blue tick to disply the 
     messages status (e.g. read or unread).

